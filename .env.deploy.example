# Copy to .env.deploy and replace every placeholder before deployment.
# Values are hostnames only: no scheme, path or port.
RELAY_HOST=relay.example.com
SHELTER_HOST=shelter.example.com
TRANSIT_HOST=transit.example.com
SUPPLY_HOST=supply.example.com
ACME_EMAIL=ops@example.com
RELAY_IMAGE_TAG=submission
# Must be the exact 40-character commit deployed across all four origins.
RELAY_RELEASE_SHA=0000000000000000000000000000000000000000
