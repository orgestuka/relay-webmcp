{
  "name": "relay-webmcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22.0.0 <23",
    "npm": ">=10"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n relay,shelter,transit,supply \"npm run dev -w @relay/command\" \"npm run dev -w @relay/shelter-grid\" \"npm run dev -w @relay/transit-ops\" \"npm run dev -w @relay/supply-hub\"",
    "build": "npm run build -w @relay/command && npm run build -w @relay/shelter-grid && npm run build -w @relay/transit-ops && npm run build -w @relay/supply-hub",
    "test": "vitest run",
    "smoke": "node --experimental-strip-types scripts/integrity-smoke.ts",
    "audit:release": "node --experimental-strip-types scripts/release-audit.ts",
    "check:scripts": "node scripts/check-script-syntax.mjs",
    "check:release-surface": "node scripts/check-release-surface.mjs",
    "check:origin-isolation": "node scripts/origin-isolation-check.mjs",
    "check:security-headers": "node scripts/security-headers-check.mjs",
    "check:release-provenance": "node scripts/release-provenance-check.mjs",
    "check:audit-closure": "node scripts/audit-closure-check.mjs",
    "check:bridge-authority": "node scripts/bridge-authority-check.mjs",
    "check:clean-release": "node scripts/clean-release-gate.mjs",
    "typecheck": "tsc -p tsconfig.check.json --noEmit",
    "verify": "npm run check:scripts && npm run check:release-surface && npm run check:origin-isolation && npm run check:security-headers && npm run check:release-provenance && npm run check:audit-closure && npm run check:bridge-authority && npm run typecheck && npm run smoke && npm run audit:release && npm test && npm run build",
    "deploy:check": "npm run check:scripts && npm run check:release-surface && npm run check:origin-isolation && npm run check:security-headers && npm run check:release-provenance && npm run check:audit-closure && npm run check:bridge-authority && node scripts/deploy-preflight.mjs .env.deploy",
    "deploy:check:dns": "npm run check:scripts && npm run check:release-surface && npm run check:origin-isolation && npm run check:security-headers && npm run check:release-provenance && npm run check:audit-closure && npm run check:bridge-authority && node scripts/deploy-preflight.mjs .env.deploy --dns",
    "deploy:smoke": "node scripts/deployment-smoke.mjs .env.deploy",
    "gate:source": "node scripts/release-gate.mjs",
    "gate:release": "node scripts/release-gate.mjs --full --dns"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "concurrently": "9.2.1",
    "typescript": "5.8.3",
    "vite": "7.1.3",
    "vitest": "3.2.4"
  }
}
